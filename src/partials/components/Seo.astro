---
// src/partials/components/Seo.astro - INTÉGRATION CompanyData

import { companyInfos, getSchemaForPage, getSeoKeywords } from '@js/data/CompanyData';

export interface Props {
  title: string;
  description: string;
  url: string;
  image?: string;
  pageType?: 'home' | 'entreprise' | 'general';
}

const {
  title,
  description,
  url,
  image = "/img/og-home.webp",
  pageType = 'home'
} = Astro.props;

// 🎯 RÉCUPÉRATION DONNÉES OPTIMISÉES
const schemaData = getSchemaForPage(pageType);
const keywords = getSeoKeywords(pageType === 'home' ? 'all' : pageType);
---

<!-- META TAGS OPTIMISÉS -->
<title>{title}</title>
<meta name="description" content={description} />
<meta name="keywords" content={keywords.join(', ')} />

<!-- Schema.org optimisé par page -->
<script type="application/ld+json" set:html={JSON.stringify(schemaData)}></script>
